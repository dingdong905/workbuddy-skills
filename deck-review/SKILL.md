---
name: deck-review
description: "审阅、评分、验收并修复 PPT、PPTX 或 slide deck 的内容、证据、叙事、视觉、无障碍和可编辑性。Use when 用户要求 review、QA、挑错、评分、压缩、修复溢出或检查演示是否可交付；普通新建演示无需单独触发。"
license: MIT
metadata:
  architecture: "qa-gate"
  language: "zh-CN"
triggers:
  - PPT审阅
  - PPT评审
  - 检查PPT
  - PPT修复
agent_created: true
---

# PPT 审阅与修复

本技能是独立质量门。与系统 `tencent-pptx` 组合使用：先读取和渲染整个演示，再做静态检查与视觉检查。只在用户要求审阅/修复，或演示即将交付且风险较高时加载。

## 审阅顺序

1. 明确演示的受众、目标、场景和品类；没有这些信息时只能评价通用质量。
2. 渲染全部页面并生成缩略图总览。不要只读文本或只看封面。
3. 运行 `scripts/deck_audit.py <deck.pptx> --mode live|read --json`，把结果作为启发式证据。
4. 按 [评分规则](references/rubric.md) 检查内容、证据、叙事、压缩、视觉、数据、可编辑性和演讲支持。
5. 输出按严重度排序的 `qa-report`。若用户要求修复，优先处理 P0、P1，重新渲染受影响页面并复查。

## 不可只靠自动检查判断的事项

- 结论是否被证据支持，引用是否真实且与主张相符
- 一页是否只有一个清晰任务，跨页叙事是否连贯
- 视觉锚点、阅读顺序、图像意义和模板匹配
- 表格是否保留了正确口径，图表是否造成误导
- 小字号是否真的只是合法脚注，而不是正文被迫缩小

## 交付门槛

- P0：阻止交付。包括文件不可打开、关键内容缺失、虚构/错误证据、严重裁切、整页截图冒充可编辑内容。
- P1：原则上必须修复。包括核心结论无依据、密度导致不可读、关键图表误导、主要版式破坏。
- P2：应修复或显式接受。包括层级、节奏、引用呈现、对比度、轻微一致性问题。
- P3：可选润色，不应为追求自动分数破坏用户模板。

自动脚本通过不代表成品合格。用户未要求修改时只报告问题，不改变文件。

